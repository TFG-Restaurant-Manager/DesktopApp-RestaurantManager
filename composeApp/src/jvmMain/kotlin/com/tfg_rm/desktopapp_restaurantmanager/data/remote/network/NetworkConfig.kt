package com.tfg_rm.desktopapp_restaurantmanager.data.remote.network

object NetworkConfig {
    const val HOST = "define-love-attention-eve.trycloudflare.com"
    const val BASE_URL = "https://$HOST/"
    const val WS_URL = "wss://$HOST/"
}